export default {
	data(){
		return {
			$_loading:''
		}
	},
	created(){
		// this.$_loading = this.$loading({
  //         lock: true,
  //         text: 'Loading',
  //         spinner: 'el-icon-loading',
  //         background: 'rgba(0, 0, 0, 0.7)'
  //       });
	},
	mounted(){
		// setTimeout(() => {
		//   this.$_loading.close();
		// }, 2000);
	}
}